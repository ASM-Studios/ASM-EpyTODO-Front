import React from "react";

const UpdateUser = ({ id }) => {

    return (
        <div className={"App-header"}>
            <h1 className={"pixel"}>User update</h1>
        </div>
    )
}

export default UpdateUser